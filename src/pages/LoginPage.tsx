import React from 'react';
import Layout from '../components/ui/Layout';

const LoginPage = () => {
    //이곳에 로그인 페이지의 내용 작성
    return (
            <Layout>
            <h1>로그인 페이지</h1>
            {/* 로그인 폼 등의 컴포넌트를 추가하세요 */}
            </Layout>
    );
};

export default LoginPage;